# Zotero Book Splitter

一款帮助读者整理长篇 PDF 和 EPUB 的 Zotero 9 插件，适合论文集、选集和篇幅较长的著作。

PDF 可以读取原生目录书签、通过你配置的 AI 接口识别目录，或在本地扫描目录页。EPUB 可以直接读取 EPUB 3 导航目录或 EPUB 2 NCX 目录，也支持多个章节共用同一个 XHTML 文件。确认章节标题、作者和页码后，可将选中的章节生成：

- 可单独引用的 Zotero 文献条目和章节 PDF / EPUB；
- 原书条目下的独立章节 PDF / EPUB；
- PDF 原生目录书签（适用于缺少书签的文件）。

原书记录、笔记和完整 PDF / EPUB 会保留，不会被覆盖。你可以只处理需要的章节；重复执行时，即使检测到同名章节，也会按你的操作再次创建副本。新生成的独立条目默认不会加入母本所在分类，除非你在预览中主动勾选。

EPUB 拆分会保留原有样式、图片、字体，以及章节引用的注释等内部链接文档。原书没有可靠的印刷页码映射时，章节位置只用于预览，不会误写进 Zotero 引用的 `pages` 字段。受 DRM 保护的 EPUB 不支持拆分。

## 安装

1. 下载 [`zoterobooksplitter0.14.0.xpi`](https://github.com/airalinknowledge/zotero-book-splitter/raw/main/zoterobooksplitter0.14.0.xpi)。
2. 打开 Zotero，进入“设置 → 高级 → 扩展”。
3. 点击齿轮按钮，选择“从文件安装附加组件”，然后选择下载的 XPI。
4. 按提示重启 Zotero。

适用版本：Zotero 9。

## 使用提示

- PDF 自动识别结果会受排版、扫描质量和目录格式影响；EPUB 主要依赖电子书自身目录。生成前请检查章节名称、作者和页码。
- AI 识别需要你自行配置兼容的 API。PDF 内容是否发送给第三方服务，取决于你选择的识别方式和服务商；介意隐私时可使用本地扫描或原生书签识别。
- 本插件目前处于小范围内测阶段。建议先在少量文献上试用，并保留原文件备份。

## 文件说明

- `zoterobooksplitter0.14.0.xpi`：可直接安装的插件。
- `zotero-book-splitter-0.14.0-source.zip`：完整源代码及随包资源。
- `src/`：便于在线查看的主要源代码。
- `updates.json`：Zotero 自动更新信息。

## 反馈

遇到识别错误、页码偏移或兼容性问题，可在本仓库提交 Issue。请尽量说明 Zotero 版本、文件格式、PDF 类型（电子版或扫描版）或 EPUB 目录情况，以及复现步骤；请勿上传含隐私或版权受限内容的文件。

## 许可说明

本项目目前尚未附加开源许可证。除法律另有规定外，代码与资源的复制、修改及再发布需取得作者许可。插件包含的第三方资源保留其原始许可文件，详见源码包中的 `content/pdfjs/`。
